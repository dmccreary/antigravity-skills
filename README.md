# WARNING

As reported by users on r/google_antigravity, the IDE currently feeds every file in your skills folder into the context window for every prompt, regardless of complexity. If your skills folder is large, a simple "hello world" can technically "cost" hundreds of thousands of tokens, instantly burning through your weekly quota.

DO NOT USE SKILLS IN ANTIGRAVITY!!!